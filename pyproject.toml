[project]
name = "boids"
version = "1.2.0"
requires-python = ">=3.14"

[project.scripts]
boids = "boids.main:main"

[build-system]
requires = ["setuptools>=80", "pygame-ce>=2.5.8"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
